int main()

{
const SIZE = 5; 
int list[SIZE];
int temp;

for (a = 1; a < SIZE; a++)
  for (b = 0; b < SIZE - a; b++)
    {
     if (list[b] > list [b+1])
      { 
        temp = list [b];
        list[b+1] = list[b];
        list[b]=temp;
       }
 return(0);
}
