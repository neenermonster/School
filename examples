#include <iostream>
        // Goes into every page for now. 
        //This is a regular template for how most functions will be structured

        //#include <iomanip>
          //I use this a lot for setting precision within my floats

        //#include <cmath>
          // For using power to a variable, square rooting something
          
        //#include <string>
          // Good for any string activity
        
using namespace std;

int main() {
````````````````````````````````````````````````````````````
cout << fixed << showpoint << setprecision(2);
        //to set how many digits places in a float use this
````````````````````````````````````````````````````````````
        int sum = 0;
        do
        {
           sum = sum + num % 10;               
           num = num / 10;
        }
        while (num > 0);
        
````````````````````````````````````````````````````````````   
        while (numberCounter <= NUM_LOOPS)
   {
       cout << head1 << numberCounter << endl << head2 << numberCounter * byTwo << endl;
       cout << head3 << numberCounter * byTen << endl << endl;
       numberCounter++;
   }
````````````````````````````````````````````````````````````

  return 0;
}
